<view class="page">
  <view class="row" wx:for="{{rows}}" wx:key="key" data-idx="{{index}}" bindtap="tapRow">
    <text>{{item.label}}</text>
    <text>›</text>
  </view>
  <view class="logout" bindtap="logout">退出登录</view>
</view>
